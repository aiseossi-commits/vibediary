## Tasks

- [ ] T1. `aiProcessor.ts`에 `buildEmbeddingText(rawText, summary)` 함수 추가
- [ ] T2. `recordPipeline.ts` 두 곳의 임베딩 소스를 `buildEmbeddingText` 사용으로 변경
- [ ] T3. `offlineQueue.ts` 임베딩 소스를 `buildEmbeddingText` 사용으로 변경
- [ ] T4. `SettingsScreen.tsx` 재색인 로직을 `buildEmbeddingText` 사용으로 변경
- [ ] T5. `npx tsc --noEmit` 통과 확인 후 STATE.md 업데이트 및 커밋
